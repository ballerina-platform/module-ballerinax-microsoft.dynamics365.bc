// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;

# Business Central Standard APIs (v1.0) for Microsoft Dynamics 365 Business Central.
public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://api.businesscentral.dynamics.com/v2.0/production/api/v1.0") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Returns a list of companies
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of companies was returned successfully. 
    remote isolated function listCompanies(map<string|string[]> headers = {}, *ListCompaniesQueries queries) returns CompanyCollection|error {
        string resourcePath = string `/companies`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a company
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested company was returned successfully. 
    remote isolated function getCompany(string companyId, map<string|string[]> headers = {}, *GetCompanyQueries queries) returns Company|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of items
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of items was returned successfully. 
    remote isolated function listItems(string companyId, map<string|string[]> headers = {}, *ListItemsQueries queries) returns ItemCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an item
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The item to create or update. 
    # + return - The item was created successfully. 
    remote isolated function createItem(string companyId, ItemRequest payload, map<string|string[]> headers = {}) returns Item|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested item was returned successfully. 
    remote isolated function getItem(string companyId, string itemId, map<string|string[]> headers = {}, *GetItemQueries queries) returns Item|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + return - The item was deleted successfully. 
    remote isolated function deleteItem(string companyId, string itemId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + payload - The item to create or update. 
    # + return - The updated item was returned successfully. 
    remote isolated function updateItem(string companyId, string itemId, UpdateItemHeaders headers, ItemRequest payload) returns Item|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of pictures for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of pictures was returned successfully. 
    remote isolated function listPicturesForItem(string companyId, string itemId, map<string|string[]> headers = {}, *ListPicturesForItemQueries queries) returns PictureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/picture`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a picture for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested picture was returned successfully. 
    remote isolated function getPictureForItem(string companyId, string itemId, string pictureId, map<string|string[]> headers = {}, *GetPictureForItemQueries queries) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/picture(${getEncodedUri(pictureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a picture for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + return - The picture was deleted successfully. 
    remote isolated function deletePictureForItem(string companyId, string itemId, string pictureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/picture(${getEncodedUri(pictureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a picture for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + payload - The picture to create or update. 
    # + return - The updated picture was returned successfully. 
    remote isolated function updatePictureForItem(string companyId, string itemId, string pictureId, UpdatePictureForItemHeaders headers, PictureRequest payload) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/picture(${getEncodedUri(pictureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of default dimensions for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of default dimensions was returned successfully. 
    remote isolated function listDefaultDimensionsForItem(string companyId, string itemId, map<string|string[]> headers = {}, *ListDefaultDimensionsForItemQueries queries) returns DefaultDimensionsCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/defaultDimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a default dimension for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The default dimension was created successfully. 
    remote isolated function createDefaultDimensionsForItem(string companyId, string itemId, DefaultDimensionsRequest payload, map<string|string[]> headers = {}) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/defaultDimensions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a default dimension for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested default dimension was returned successfully. 
    remote isolated function getDefaultDimensionsForItem(string companyId, string itemId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}, *GetDefaultDimensionsForItemQueries queries) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a default dimension for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + return - The default dimension was deleted successfully. 
    remote isolated function deleteDefaultDimensionsForItem(string companyId, string itemId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a default dimension for an item
    #
    # + companyId - The unique identifier of the company.
    # + itemId - The unique identifier of the item.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The updated default dimension was returned successfully. 
    remote isolated function updateDefaultDimensionsForItem(string companyId, string itemId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, UpdateDefaultDimensionsForItemHeaders headers, DefaultDimensionsRequest payload) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/items(${getEncodedUri(itemId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of pictures
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of pictures was returned successfully. 
    remote isolated function listPictures(string companyId, map<string|string[]> headers = {}, *ListPicturesQueries queries) returns PictureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/picture`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a picture
    #
    # + companyId - The unique identifier of the company.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested picture was returned successfully. 
    remote isolated function getPicture(string companyId, string pictureId, map<string|string[]> headers = {}, *GetPictureQueries queries) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/picture(${getEncodedUri(pictureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a picture
    #
    # + companyId - The unique identifier of the company.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + return - The picture was deleted successfully. 
    remote isolated function deletePicture(string companyId, string pictureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/picture(${getEncodedUri(pictureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a picture
    #
    # + companyId - The unique identifier of the company.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + payload - The picture to create or update. 
    # + return - The updated picture was returned successfully. 
    remote isolated function updatePicture(string companyId, string pictureId, UpdatePictureHeaders headers, PictureRequest payload) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/picture(${getEncodedUri(pictureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of default dimensions
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of default dimensions was returned successfully. 
    remote isolated function listDefaultDimensions(string companyId, map<string|string[]> headers = {}, *ListDefaultDimensionsQueries queries) returns DefaultDimensionsCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/defaultDimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a default dimension
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The default dimension was created successfully. 
    remote isolated function createDefaultDimensions(string companyId, DefaultDimensionsRequest payload, map<string|string[]> headers = {}) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/defaultDimensions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a default dimension
    #
    # + companyId - The unique identifier of the company.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested default dimension was returned successfully. 
    remote isolated function getDefaultDimensions(string companyId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}, *GetDefaultDimensionsQueries queries) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a default dimension
    #
    # + companyId - The unique identifier of the company.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + return - The default dimension was deleted successfully. 
    remote isolated function deleteDefaultDimensions(string companyId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a default dimension
    #
    # + companyId - The unique identifier of the company.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The updated default dimension was returned successfully. 
    remote isolated function updateDefaultDimensions(string companyId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, UpdateDefaultDimensionsHeaders headers, DefaultDimensionsRequest payload) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customers
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customers was returned successfully. 
    remote isolated function listCustomers(string companyId, map<string|string[]> headers = {}, *ListCustomersQueries queries) returns CustomerCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a customer
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The customer to create or update. 
    # + return - The customer was created successfully. 
    remote isolated function createCustomer(string companyId, CustomerRequest payload, map<string|string[]> headers = {}) returns Customer|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer was returned successfully. 
    remote isolated function getCustomer(string companyId, string customerId, map<string|string[]> headers = {}, *GetCustomerQueries queries) returns Customer|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + return - The customer was deleted successfully. 
    remote isolated function deleteCustomer(string companyId, string customerId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + payload - The customer to create or update. 
    # + return - The updated customer was returned successfully. 
    remote isolated function updateCustomer(string companyId, string customerId, UpdateCustomerHeaders headers, CustomerRequest payload) returns Customer|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customer financial details for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer financial details was returned successfully. 
    remote isolated function listCustomerFinancialDetailsForCustomer(string companyId, string customerId, map<string|string[]> headers = {}, *ListCustomerFinancialDetailsForCustomerQueries queries) returns CustomerFinancialDetailCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/customerFinancialDetails`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a customer financial detail for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + customerFinancialDetailId - The unique identifier of the customer financial detail.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer financial detail was returned successfully. 
    remote isolated function getCustomerFinancialDetailForCustomer(string companyId, string customerId, string customerFinancialDetailId, map<string|string[]> headers = {}, *GetCustomerFinancialDetailForCustomerQueries queries) returns CustomerFinancialDetail|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/customerFinancialDetails(${getEncodedUri(customerFinancialDetailId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of pictures for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of pictures was returned successfully. 
    remote isolated function listPicturesForCustomer(string companyId, string customerId, map<string|string[]> headers = {}, *ListPicturesForCustomerQueries queries) returns PictureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/picture`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a picture for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested picture was returned successfully. 
    remote isolated function getPictureForCustomer(string companyId, string customerId, string pictureId, map<string|string[]> headers = {}, *GetPictureForCustomerQueries queries) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/picture(${getEncodedUri(pictureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a picture for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + return - The picture was deleted successfully. 
    remote isolated function deletePictureForCustomer(string companyId, string customerId, string pictureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/picture(${getEncodedUri(pictureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a picture for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + payload - The picture to create or update. 
    # + return - The updated picture was returned successfully. 
    remote isolated function updatePictureForCustomer(string companyId, string customerId, string pictureId, UpdatePictureForCustomerHeaders headers, PictureRequest payload) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/picture(${getEncodedUri(pictureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of default dimensions for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of default dimensions was returned successfully. 
    remote isolated function listDefaultDimensionsForCustomer(string companyId, string customerId, map<string|string[]> headers = {}, *ListDefaultDimensionsForCustomerQueries queries) returns DefaultDimensionsCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/defaultDimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a default dimension for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The default dimension was created successfully. 
    remote isolated function createDefaultDimensionsForCustomer(string companyId, string customerId, DefaultDimensionsRequest payload, map<string|string[]> headers = {}) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/defaultDimensions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a default dimension for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested default dimension was returned successfully. 
    remote isolated function getDefaultDimensionsForCustomer(string companyId, string customerId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}, *GetDefaultDimensionsForCustomerQueries queries) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a default dimension for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + return - The default dimension was deleted successfully. 
    remote isolated function deleteDefaultDimensionsForCustomer(string companyId, string customerId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a default dimension for a customer
    #
    # + companyId - The unique identifier of the company.
    # + customerId - The unique identifier of the customer.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The updated default dimension was returned successfully. 
    remote isolated function updateDefaultDimensionsForCustomer(string companyId, string customerId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, UpdateDefaultDimensionsForCustomerHeaders headers, DefaultDimensionsRequest payload) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customers(${getEncodedUri(customerId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customer financial details
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer financial details was returned successfully. 
    remote isolated function listCustomerFinancialDetails(string companyId, map<string|string[]> headers = {}, *ListCustomerFinancialDetailsQueries queries) returns CustomerFinancialDetailCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerFinancialDetails`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a customer financial detail
    #
    # + companyId - The unique identifier of the company.
    # + customerFinancialDetailId - The unique identifier of the customer financial detail.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer financial detail was returned successfully. 
    remote isolated function getCustomerFinancialDetail(string companyId, string customerFinancialDetailId, map<string|string[]> headers = {}, *GetCustomerFinancialDetailQueries queries) returns CustomerFinancialDetail|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerFinancialDetails(${getEncodedUri(customerFinancialDetailId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of vendors
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of vendors was returned successfully. 
    remote isolated function listVendors(string companyId, map<string|string[]> headers = {}, *ListVendorsQueries queries) returns VendorCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a vendor
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The vendor to create or update. 
    # + return - The vendor was created successfully. 
    remote isolated function createVendor(string companyId, VendorRequest payload, map<string|string[]> headers = {}) returns Vendor|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested vendor was returned successfully. 
    remote isolated function getVendor(string companyId, string vendorId, map<string|string[]> headers = {}, *GetVendorQueries queries) returns Vendor|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + return - The vendor was deleted successfully. 
    remote isolated function deleteVendor(string companyId, string vendorId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + payload - The vendor to create or update. 
    # + return - The updated vendor was returned successfully. 
    remote isolated function updateVendor(string companyId, string vendorId, UpdateVendorHeaders headers, VendorRequest payload) returns Vendor|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of pictures for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of pictures was returned successfully. 
    remote isolated function listPicturesForVendor(string companyId, string vendorId, map<string|string[]> headers = {}, *ListPicturesForVendorQueries queries) returns PictureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/picture`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a picture for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested picture was returned successfully. 
    remote isolated function getPictureForVendor(string companyId, string vendorId, string pictureId, map<string|string[]> headers = {}, *GetPictureForVendorQueries queries) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/picture(${getEncodedUri(pictureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a picture for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + return - The picture was deleted successfully. 
    remote isolated function deletePictureForVendor(string companyId, string vendorId, string pictureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/picture(${getEncodedUri(pictureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a picture for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + payload - The picture to create or update. 
    # + return - The updated picture was returned successfully. 
    remote isolated function updatePictureForVendor(string companyId, string vendorId, string pictureId, UpdatePictureForVendorHeaders headers, PictureRequest payload) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/picture(${getEncodedUri(pictureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of default dimensions for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of default dimensions was returned successfully. 
    remote isolated function listDefaultDimensionsForVendor(string companyId, string vendorId, map<string|string[]> headers = {}, *ListDefaultDimensionsForVendorQueries queries) returns DefaultDimensionsCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/defaultDimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a default dimension for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The default dimension was created successfully. 
    remote isolated function createDefaultDimensionsForVendor(string companyId, string vendorId, DefaultDimensionsRequest payload, map<string|string[]> headers = {}) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/defaultDimensions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a default dimension for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested default dimension was returned successfully. 
    remote isolated function getDefaultDimensionsForVendor(string companyId, string vendorId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}, *GetDefaultDimensionsForVendorQueries queries) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a default dimension for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + return - The default dimension was deleted successfully. 
    remote isolated function deleteDefaultDimensionsForVendor(string companyId, string vendorId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a default dimension for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + vendorId - The unique identifier of the vendor.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The updated default dimension was returned successfully. 
    remote isolated function updateDefaultDimensionsForVendor(string companyId, string vendorId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, UpdateDefaultDimensionsForVendorHeaders headers, DefaultDimensionsRequest payload) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendors(${getEncodedUri(vendorId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of company information records
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of company information records was returned successfully. 
    remote isolated function listCompanyInformation(string companyId, map<string|string[]> headers = {}, *ListCompanyInformationQueries queries) returns CompanyInformationCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/companyInformation`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a company information record
    #
    # + companyId - The unique identifier of the company.
    # + companyInformationId - The unique identifier of the company information.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested company information record was returned successfully. 
    remote isolated function getCompanyInformation(string companyId, string companyInformationId, map<string|string[]> headers = {}, *GetCompanyInformationQueries queries) returns CompanyInformation|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/companyInformation(${getEncodedUri(companyInformationId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a company information record
    #
    # + companyId - The unique identifier of the company.
    # + companyInformationId - The unique identifier of the company information.
    # + headers - Headers to be sent with the request 
    # + payload - The company information to create or update. 
    # + return - The updated company information record was returned successfully. 
    remote isolated function updateCompanyInformation(string companyId, string companyInformationId, UpdateCompanyInformationHeaders headers, CompanyInformationRequest payload) returns CompanyInformation|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/companyInformation(${getEncodedUri(companyInformationId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of sales invoices
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales invoices was returned successfully. 
    remote isolated function listSalesInvoices(string companyId, map<string|string[]> headers = {}, *ListSalesInvoicesQueries queries) returns SalesInvoiceCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice to create or update. 
    # + return - The sales invoice was created successfully. 
    remote isolated function createSalesInvoice(string companyId, SalesInvoiceRequest payload, map<string|string[]> headers = {}) returns SalesInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales invoice was returned successfully. 
    remote isolated function getSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}, *GetSalesInvoiceQueries queries) returns SalesInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The sales invoice was deleted successfully. 
    remote isolated function deleteSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice to create or update. 
    # + return - The updated sales invoice was returned successfully. 
    remote isolated function updateSalesInvoice(string companyId, string salesInvoiceId, UpdateSalesInvoiceHeaders headers, SalesInvoiceRequest payload) returns SalesInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the cancel and send action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The cancel and send action completed successfully. 
    remote isolated function cancelAndSendSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.cancelAndSend`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the cancel action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The cancel action completed successfully. 
    remote isolated function cancelSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.cancel`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the make corrective credit memo action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The make corrective credit memo action completed successfully. 
    remote isolated function makeCorrectiveCreditMemoForSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.makeCorrectiveCreditMemo`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the post and send action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The post and send action completed successfully. 
    remote isolated function postAndSendSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.postAndSend`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the post action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The post action completed successfully. 
    remote isolated function postSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.post`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the send action on a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + return - The send action completed successfully. 
    remote isolated function sendSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/Microsoft.NAV.send`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of sales invoice lines for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales invoice lines was returned successfully. 
    remote isolated function listSalesInvoiceLinesForSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}, *ListSalesInvoiceLinesForSalesInvoiceQueries queries) returns SalesInvoiceLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/salesInvoiceLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales invoice line for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice line to create or update. 
    # + return - The sales invoice line was created successfully. 
    remote isolated function createSalesInvoiceLineForSalesInvoice(string companyId, string salesInvoiceId, SalesInvoiceLineRequest payload, map<string|string[]> headers = {}) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/salesInvoiceLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales invoice line for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales invoice line was returned successfully. 
    remote isolated function getSalesInvoiceLineForSalesInvoice(string companyId, string salesInvoiceId, string salesInvoiceLineId, map<string|string[]> headers = {}, *GetSalesInvoiceLineForSalesInvoiceQueries queries) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales invoice line for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + return - The sales invoice line was deleted successfully. 
    remote isolated function deleteSalesInvoiceLineForSalesInvoice(string companyId, string salesInvoiceId, string salesInvoiceLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales invoice line for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice line to create or update. 
    # + return - The updated sales invoice line was returned successfully. 
    remote isolated function updateSalesInvoiceLineForSalesInvoice(string companyId, string salesInvoiceId, string salesInvoiceLineId, UpdateSalesInvoiceLineForSalesInvoiceHeaders headers, SalesInvoiceLineRequest payload) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of PDF documents for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of PDF documents was returned successfully. 
    remote isolated function listPdfDocumentsForSalesInvoice(string companyId, string salesInvoiceId, map<string|string[]> headers = {}, *ListPdfDocumentsForSalesInvoiceQueries queries) returns PdfDocumentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/pdfDocument`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a PDF document for a sales invoice
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceId - The unique identifier of the sales invoice.
    # + pdfDocumentId - The unique identifier of the pdf document.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested PDF document was returned successfully. 
    remote isolated function getPdfDocumentForSalesInvoice(string companyId, string salesInvoiceId, string pdfDocumentId, map<string|string[]> headers = {}, *GetPdfDocumentForSalesInvoiceQueries queries) returns PdfDocument|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoices(${getEncodedUri(salesInvoiceId)})/pdfDocument(${getEncodedUri(pdfDocumentId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of sales invoice lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales invoice lines was returned successfully. 
    remote isolated function listSalesInvoiceLines(string companyId, map<string|string[]> headers = {}, *ListSalesInvoiceLinesQueries queries) returns SalesInvoiceLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoiceLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales invoice line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice line to create or update. 
    # + return - The sales invoice line was created successfully. 
    remote isolated function createSalesInvoiceLine(string companyId, SalesInvoiceLineRequest payload, map<string|string[]> headers = {}) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoiceLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales invoice line
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales invoice line was returned successfully. 
    remote isolated function getSalesInvoiceLine(string companyId, string salesInvoiceLineId, map<string|string[]> headers = {}, *GetSalesInvoiceLineQueries queries) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales invoice line
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + return - The sales invoice line was deleted successfully. 
    remote isolated function deleteSalesInvoiceLine(string companyId, string salesInvoiceLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales invoice line
    #
    # + companyId - The unique identifier of the company.
    # + salesInvoiceLineId - The unique identifier of the sales invoice line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales invoice line to create or update. 
    # + return - The updated sales invoice line was returned successfully. 
    remote isolated function updateSalesInvoiceLine(string companyId, string salesInvoiceLineId, UpdateSalesInvoiceLineHeaders headers, SalesInvoiceLineRequest payload) returns SalesInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesInvoiceLines('${getEncodedUri(salesInvoiceLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of PDF documents
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of PDF documents was returned successfully. 
    remote isolated function listPdfDocuments(string companyId, map<string|string[]> headers = {}, *ListPdfDocumentsQueries queries) returns PdfDocumentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/pdfDocument`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a PDF document
    #
    # + companyId - The unique identifier of the company.
    # + pdfDocumentId - The unique identifier of the pdf document.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested PDF document was returned successfully. 
    remote isolated function getPdfDocument(string companyId, string pdfDocumentId, map<string|string[]> headers = {}, *GetPdfDocumentQueries queries) returns PdfDocument|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/pdfDocument(${getEncodedUri(pdfDocumentId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of customer payment journals
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer payment journals was returned successfully. 
    remote isolated function listCustomerPaymentJournals(string companyId, map<string|string[]> headers = {}, *ListCustomerPaymentJournalsQueries queries) returns CustomerPaymentJournalCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment journal to create or update. 
    # + return - The customer payment journal was created successfully. 
    remote isolated function createCustomerPaymentJournal(string companyId, CustomerPaymentJournalRequest payload, map<string|string[]> headers = {}) returns CustomerPaymentJournal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer payment journal was returned successfully. 
    remote isolated function getCustomerPaymentJournal(string companyId, string customerPaymentJournalId, map<string|string[]> headers = {}, *GetCustomerPaymentJournalQueries queries) returns CustomerPaymentJournal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + headers - Headers to be sent with the request 
    # + return - The customer payment journal was deleted successfully. 
    remote isolated function deleteCustomerPaymentJournal(string companyId, string customerPaymentJournalId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment journal to create or update. 
    # + return - The updated customer payment journal was returned successfully. 
    remote isolated function updateCustomerPaymentJournal(string companyId, string customerPaymentJournalId, UpdateCustomerPaymentJournalHeaders headers, CustomerPaymentJournalRequest payload) returns CustomerPaymentJournal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customer payments for a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer payments was returned successfully. 
    remote isolated function listCustomerPaymentsForCustomerPaymentJournal(string companyId, string customerPaymentJournalId, map<string|string[]> headers = {}, *ListCustomerPaymentsForCustomerPaymentJournalQueries queries) returns CustomerPaymentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})/customerPayments`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a customer payment for a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment to create or update. 
    # + return - The customer payment was created successfully. 
    remote isolated function createCustomerPaymentForCustomerPaymentJournal(string companyId, string customerPaymentJournalId, CustomerPaymentRequest payload, map<string|string[]> headers = {}) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})/customerPayments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a customer payment for a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer payment was returned successfully. 
    remote isolated function getCustomerPaymentForCustomerPaymentJournal(string companyId, string customerPaymentJournalId, string customerPaymentId, map<string|string[]> headers = {}, *GetCustomerPaymentForCustomerPaymentJournalQueries queries) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a customer payment for a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + return - The customer payment was deleted successfully. 
    remote isolated function deleteCustomerPaymentForCustomerPaymentJournal(string companyId, string customerPaymentJournalId, string customerPaymentId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a customer payment for a customer payment journal
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentJournalId - The unique identifier of the customer payment journal.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment to create or update. 
    # + return - The updated customer payment was returned successfully. 
    remote isolated function updateCustomerPaymentForCustomerPaymentJournal(string companyId, string customerPaymentJournalId, string customerPaymentId, UpdateCustomerPaymentForCustomerPaymentJournalHeaders headers, CustomerPaymentRequest payload) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPaymentJournals(${getEncodedUri(customerPaymentJournalId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customer payments
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer payments was returned successfully. 
    remote isolated function listCustomerPayments(string companyId, map<string|string[]> headers = {}, *ListCustomerPaymentsQueries queries) returns CustomerPaymentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPayments`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a customer payment
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment to create or update. 
    # + return - The customer payment was created successfully. 
    remote isolated function createCustomerPayment(string companyId, CustomerPaymentRequest payload, map<string|string[]> headers = {}) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPayments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a customer payment
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer payment was returned successfully. 
    remote isolated function getCustomerPayment(string companyId, string customerPaymentId, map<string|string[]> headers = {}, *GetCustomerPaymentQueries queries) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a customer payment
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + return - The customer payment was deleted successfully. 
    remote isolated function deleteCustomerPayment(string companyId, string customerPaymentId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a customer payment
    #
    # + companyId - The unique identifier of the company.
    # + customerPaymentId - The unique identifier of the customer payment.
    # + headers - Headers to be sent with the request 
    # + payload - The customer payment to create or update. 
    # + return - The updated customer payment was returned successfully. 
    remote isolated function updateCustomerPayment(string companyId, string customerPaymentId, UpdateCustomerPaymentHeaders headers, CustomerPaymentRequest payload) returns CustomerPayment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerPayments(${getEncodedUri(customerPaymentId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of general ledger accounts
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of general ledger accounts was returned successfully. 
    remote isolated function listAccounts(string companyId, map<string|string[]> headers = {}, *ListAccountsQueries queries) returns AccountCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/accounts`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a general ledger account
    #
    # + companyId - The unique identifier of the company.
    # + accountId - The unique identifier of the account.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested general ledger account was returned successfully. 
    remote isolated function getAccount(string companyId, string accountId, map<string|string[]> headers = {}, *GetAccountQueries queries) returns Account|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/accounts(${getEncodedUri(accountId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of tax groups
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of tax groups was returned successfully. 
    remote isolated function listTaxGroups(string companyId, map<string|string[]> headers = {}, *ListTaxGroupsQueries queries) returns TaxGroupCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxGroups`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a tax group
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The tax group to create or update. 
    # + return - The tax group was created successfully. 
    remote isolated function createTaxGroup(string companyId, TaxGroupRequest payload, map<string|string[]> headers = {}) returns TaxGroup|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a tax group
    #
    # + companyId - The unique identifier of the company.
    # + taxGroupId - The unique identifier of the tax group.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested tax group was returned successfully. 
    remote isolated function getTaxGroup(string companyId, string taxGroupId, map<string|string[]> headers = {}, *GetTaxGroupQueries queries) returns TaxGroup|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxGroups(${getEncodedUri(taxGroupId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a tax group
    #
    # + companyId - The unique identifier of the company.
    # + taxGroupId - The unique identifier of the tax group.
    # + headers - Headers to be sent with the request 
    # + return - The tax group was deleted successfully. 
    remote isolated function deleteTaxGroup(string companyId, string taxGroupId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxGroups(${getEncodedUri(taxGroupId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a tax group
    #
    # + companyId - The unique identifier of the company.
    # + taxGroupId - The unique identifier of the tax group.
    # + headers - Headers to be sent with the request 
    # + payload - The tax group to create or update. 
    # + return - The updated tax group was returned successfully. 
    remote isolated function updateTaxGroup(string companyId, string taxGroupId, UpdateTaxGroupHeaders headers, TaxGroupRequest payload) returns TaxGroup|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxGroups(${getEncodedUri(taxGroupId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of journals
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of journals was returned successfully. 
    remote isolated function listJournals(string companyId, map<string|string[]> headers = {}, *ListJournalsQueries queries) returns JournalCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a journal
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The journal to create or update. 
    # + return - The journal was created successfully. 
    remote isolated function createJournal(string companyId, JournalRequest payload, map<string|string[]> headers = {}) returns Journal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested journal was returned successfully. 
    remote isolated function getJournal(string companyId, string journalId, map<string|string[]> headers = {}, *GetJournalQueries queries) returns Journal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + return - The journal was deleted successfully. 
    remote isolated function deleteJournal(string companyId, string journalId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + payload - The journal to create or update. 
    # + return - The updated journal was returned successfully. 
    remote isolated function updateJournal(string companyId, string journalId, UpdateJournalHeaders headers, JournalRequest payload) returns Journal|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the post action on a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + return - The post action completed successfully. 
    remote isolated function postJournal(string companyId, string journalId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/Microsoft.NAV.post`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of journal lines for a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of journal lines was returned successfully. 
    remote isolated function listJournalLinesForJournal(string companyId, string journalId, map<string|string[]> headers = {}, *ListJournalLinesForJournalQueries queries) returns JournalLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a journal line for a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + headers - Headers to be sent with the request 
    # + payload - The journal line to create or update. 
    # + return - The journal line was created successfully. 
    remote isolated function createJournalLineForJournal(string companyId, string journalId, JournalLineRequest payload, map<string|string[]> headers = {}) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a journal line for a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested journal line was returned successfully. 
    remote isolated function getJournalLineForJournal(string companyId, string journalId, string journalLineId, map<string|string[]> headers = {}, *GetJournalLineForJournalQueries queries) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a journal line for a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + return - The journal line was deleted successfully. 
    remote isolated function deleteJournalLineForJournal(string companyId, string journalId, string journalLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a journal line for a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + payload - The journal line to create or update. 
    # + return - The updated journal line was returned successfully. 
    remote isolated function updateJournalLineForJournal(string companyId, string journalId, string journalLineId, UpdateJournalLineForJournalHeaders headers, JournalLineRequest payload) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of attachments for a journal line of a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of attachments was returned successfully. 
    remote isolated function listAttachmentsForJournalLineForJournal(string companyId, string journalId, string journalLineId, map<string|string[]> headers = {}, *ListAttachmentsForJournalLineForJournalQueries queries) returns AttachmentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})/attachments`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an attachment for a journal line of a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The attachment was created successfully. 
    remote isolated function createAttachmentForJournalLineForJournal(string companyId, string journalId, string journalLineId, AttachmentRequest payload, map<string|string[]> headers = {}) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})/attachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an attachment for a journal line of a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested attachment was returned successfully. 
    remote isolated function getAttachmentForJournalLineForJournal(string companyId, string journalId, string journalLineId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}, *GetAttachmentForJournalLineForJournalQueries queries) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an attachment for a journal line of a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + return - The attachment was deleted successfully. 
    remote isolated function deleteAttachmentForJournalLineForJournal(string companyId, string journalId, string journalLineId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an attachment for a journal line of a journal
    #
    # + companyId - The unique identifier of the company.
    # + journalId - The unique identifier of the journal.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The updated attachment was returned successfully. 
    remote isolated function updateAttachmentForJournalLineForJournal(string companyId, string journalId, string journalLineId, string attachmentsParentId, string attachmentsId, UpdateAttachmentForJournalLineForJournalHeaders headers, AttachmentRequest payload) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journals(${getEncodedUri(journalId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of journal lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of journal lines was returned successfully. 
    remote isolated function listJournalLines(string companyId, map<string|string[]> headers = {}, *ListJournalLinesQueries queries) returns JournalLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a journal line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The journal line to create or update. 
    # + return - The journal line was created successfully. 
    remote isolated function createJournalLine(string companyId, JournalLineRequest payload, map<string|string[]> headers = {}) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested journal line was returned successfully. 
    remote isolated function getJournalLine(string companyId, string journalLineId, map<string|string[]> headers = {}, *GetJournalLineQueries queries) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + return - The journal line was deleted successfully. 
    remote isolated function deleteJournalLine(string companyId, string journalLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + payload - The journal line to create or update. 
    # + return - The updated journal line was returned successfully. 
    remote isolated function updateJournalLine(string companyId, string journalLineId, UpdateJournalLineHeaders headers, JournalLineRequest payload) returns JournalLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of attachments for a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of attachments was returned successfully. 
    remote isolated function listAttachmentsForJournalLine(string companyId, string journalLineId, map<string|string[]> headers = {}, *ListAttachmentsForJournalLineQueries queries) returns AttachmentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})/attachments`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an attachment for a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The attachment was created successfully. 
    remote isolated function createAttachmentForJournalLine(string companyId, string journalLineId, AttachmentRequest payload, map<string|string[]> headers = {}) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})/attachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an attachment for a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested attachment was returned successfully. 
    remote isolated function getAttachmentForJournalLine(string companyId, string journalLineId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}, *GetAttachmentForJournalLineQueries queries) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an attachment for a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + return - The attachment was deleted successfully. 
    remote isolated function deleteAttachmentForJournalLine(string companyId, string journalLineId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an attachment for a journal line
    #
    # + companyId - The unique identifier of the company.
    # + journalLineId - The unique identifier of the journal line.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The updated attachment was returned successfully. 
    remote isolated function updateAttachmentForJournalLine(string companyId, string journalLineId, string attachmentsParentId, string attachmentsId, UpdateAttachmentForJournalLineHeaders headers, AttachmentRequest payload) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/journalLines(${getEncodedUri(journalLineId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of attachments
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of attachments was returned successfully. 
    remote isolated function listAttachments(string companyId, map<string|string[]> headers = {}, *ListAttachmentsQueries queries) returns AttachmentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/attachments`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an attachment
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The attachment was created successfully. 
    remote isolated function createAttachment(string companyId, AttachmentRequest payload, map<string|string[]> headers = {}) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/attachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an attachment
    #
    # + companyId - The unique identifier of the company.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested attachment was returned successfully. 
    remote isolated function getAttachment(string companyId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}, *GetAttachmentQueries queries) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an attachment
    #
    # + companyId - The unique identifier of the company.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + return - The attachment was deleted successfully. 
    remote isolated function deleteAttachment(string companyId, string attachmentsParentId, string attachmentsId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an attachment
    #
    # + companyId - The unique identifier of the company.
    # + attachmentsParentId - The parent id of the attachments.
    # + attachmentsId - The unique identifier of the attachments.
    # + headers - Headers to be sent with the request 
    # + payload - The attachments to create or update. 
    # + return - The updated attachment was returned successfully. 
    remote isolated function updateAttachment(string companyId, string attachmentsParentId, string attachmentsId, UpdateAttachmentHeaders headers, AttachmentRequest payload) returns Attachment|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/attachments(${getEncodedUri(attachmentsParentId)},${getEncodedUri(attachmentsId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of employees
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of employees was returned successfully. 
    remote isolated function listEmployees(string companyId, map<string|string[]> headers = {}, *ListEmployeesQueries queries) returns EmployeeCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an employee
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The employee to create or update. 
    # + return - The employee was created successfully. 
    remote isolated function createEmployee(string companyId, EmployeeRequest payload, map<string|string[]> headers = {}) returns Employee|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested employee was returned successfully. 
    remote isolated function getEmployee(string companyId, string employeeId, map<string|string[]> headers = {}, *GetEmployeeQueries queries) returns Employee|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + return - The employee was deleted successfully. 
    remote isolated function deleteEmployee(string companyId, string employeeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + payload - The employee to create or update. 
    # + return - The updated employee was returned successfully. 
    remote isolated function updateEmployee(string companyId, string employeeId, UpdateEmployeeHeaders headers, EmployeeRequest payload) returns Employee|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of pictures for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of pictures was returned successfully. 
    remote isolated function listPicturesForEmployee(string companyId, string employeeId, map<string|string[]> headers = {}, *ListPicturesForEmployeeQueries queries) returns PictureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/picture`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a picture for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested picture was returned successfully. 
    remote isolated function getPictureForEmployee(string companyId, string employeeId, string pictureId, map<string|string[]> headers = {}, *GetPictureForEmployeeQueries queries) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/picture(${getEncodedUri(pictureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a picture for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + return - The picture was deleted successfully. 
    remote isolated function deletePictureForEmployee(string companyId, string employeeId, string pictureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/picture(${getEncodedUri(pictureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a picture for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + pictureId - The unique identifier of the picture.
    # + headers - Headers to be sent with the request 
    # + payload - The picture to create or update. 
    # + return - The updated picture was returned successfully. 
    remote isolated function updatePictureForEmployee(string companyId, string employeeId, string pictureId, UpdatePictureForEmployeeHeaders headers, PictureRequest payload) returns Picture|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/picture(${getEncodedUri(pictureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of default dimensions for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of default dimensions was returned successfully. 
    remote isolated function listDefaultDimensionsForEmployee(string companyId, string employeeId, map<string|string[]> headers = {}, *ListDefaultDimensionsForEmployeeQueries queries) returns DefaultDimensionsCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/defaultDimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a default dimension for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The default dimension was created successfully. 
    remote isolated function createDefaultDimensionsForEmployee(string companyId, string employeeId, DefaultDimensionsRequest payload, map<string|string[]> headers = {}) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/defaultDimensions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a default dimension for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested default dimension was returned successfully. 
    remote isolated function getDefaultDimensionsForEmployee(string companyId, string employeeId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}, *GetDefaultDimensionsForEmployeeQueries queries) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a default dimension for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + return - The default dimension was deleted successfully. 
    remote isolated function deleteDefaultDimensionsForEmployee(string companyId, string employeeId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a default dimension for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + defaultDimensionsParentId - The parent id of the default dimensions.
    # + defaultDimensionsDimensionId - The dimension id of the default dimensions.
    # + headers - Headers to be sent with the request 
    # + payload - The default dimensions to create or update. 
    # + return - The updated default dimension was returned successfully. 
    remote isolated function updateDefaultDimensionsForEmployee(string companyId, string employeeId, string defaultDimensionsParentId, string defaultDimensionsDimensionId, UpdateDefaultDimensionsForEmployeeHeaders headers, DefaultDimensionsRequest payload) returns DefaultDimensions|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/defaultDimensions(${getEncodedUri(defaultDimensionsParentId)},${getEncodedUri(defaultDimensionsDimensionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of time registration entries for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of time registration entries was returned successfully. 
    remote isolated function listTimeRegistrationEntriesForEmployee(string companyId, string employeeId, map<string|string[]> headers = {}, *ListTimeRegistrationEntriesForEmployeeQueries queries) returns TimeRegistrationEntryCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/timeRegistrationEntries`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a time registration entry for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + headers - Headers to be sent with the request 
    # + payload - The time registration entry to create or update. 
    # + return - The time registration entry was created successfully. 
    remote isolated function createTimeRegistrationEntryForEmployee(string companyId, string employeeId, TimeRegistrationEntryRequest payload, map<string|string[]> headers = {}) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/timeRegistrationEntries`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a time registration entry for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested time registration entry was returned successfully. 
    remote isolated function getTimeRegistrationEntryForEmployee(string companyId, string employeeId, string timeRegistrationEntryId, map<string|string[]> headers = {}, *GetTimeRegistrationEntryForEmployeeQueries queries) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a time registration entry for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + return - The time registration entry was deleted successfully. 
    remote isolated function deleteTimeRegistrationEntryForEmployee(string companyId, string employeeId, string timeRegistrationEntryId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a time registration entry for an employee
    #
    # + companyId - The unique identifier of the company.
    # + employeeId - The unique identifier of the employee.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + payload - The time registration entry to create or update. 
    # + return - The updated time registration entry was returned successfully. 
    remote isolated function updateTimeRegistrationEntryForEmployee(string companyId, string employeeId, string timeRegistrationEntryId, UpdateTimeRegistrationEntryForEmployeeHeaders headers, TimeRegistrationEntryRequest payload) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/employees(${getEncodedUri(employeeId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of time registration entries
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of time registration entries was returned successfully. 
    remote isolated function listTimeRegistrationEntries(string companyId, map<string|string[]> headers = {}, *ListTimeRegistrationEntriesQueries queries) returns TimeRegistrationEntryCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/timeRegistrationEntries`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a time registration entry
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The time registration entry to create or update. 
    # + return - The time registration entry was created successfully. 
    remote isolated function createTimeRegistrationEntry(string companyId, TimeRegistrationEntryRequest payload, map<string|string[]> headers = {}) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/timeRegistrationEntries`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a time registration entry
    #
    # + companyId - The unique identifier of the company.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested time registration entry was returned successfully. 
    remote isolated function getTimeRegistrationEntry(string companyId, string timeRegistrationEntryId, map<string|string[]> headers = {}, *GetTimeRegistrationEntryQueries queries) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a time registration entry
    #
    # + companyId - The unique identifier of the company.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + return - The time registration entry was deleted successfully. 
    remote isolated function deleteTimeRegistrationEntry(string companyId, string timeRegistrationEntryId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a time registration entry
    #
    # + companyId - The unique identifier of the company.
    # + timeRegistrationEntryId - The unique identifier of the time registration entry.
    # + headers - Headers to be sent with the request 
    # + payload - The time registration entry to create or update. 
    # + return - The updated time registration entry was returned successfully. 
    remote isolated function updateTimeRegistrationEntry(string companyId, string timeRegistrationEntryId, UpdateTimeRegistrationEntryHeaders headers, TimeRegistrationEntryRequest payload) returns TimeRegistrationEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/timeRegistrationEntries(${getEncodedUri(timeRegistrationEntryId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of general ledger entries
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of general ledger entries was returned successfully. 
    remote isolated function listGeneralLedgerEntries(string companyId, map<string|string[]> headers = {}, *ListGeneralLedgerEntriesQueries queries) returns GeneralLedgerEntryCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntries`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a general ledger entry
    #
    # + companyId - The unique identifier of the company.
    # + generalLedgerEntryId - The unique identifier of the general ledger entry.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested general ledger entry was returned successfully. 
    remote isolated function getGeneralLedgerEntry(string companyId, int:Signed32 generalLedgerEntryId, map<string|string[]> headers = {}, *GetGeneralLedgerEntryQueries queries) returns GeneralLedgerEntry|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntries(${getEncodedUri(generalLedgerEntryId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of currencies
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of currencies was returned successfully. 
    remote isolated function listCurrencies(string companyId, map<string|string[]> headers = {}, *ListCurrenciesQueries queries) returns CurrencyCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/currencies`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a currency
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The currency to create or update. 
    # + return - The currency was created successfully. 
    remote isolated function createCurrency(string companyId, CurrencyRequest payload, map<string|string[]> headers = {}) returns Currency|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/currencies`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a currency
    #
    # + companyId - The unique identifier of the company.
    # + currencyId - The unique identifier of the currency.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested currency was returned successfully. 
    remote isolated function getCurrency(string companyId, string currencyId, map<string|string[]> headers = {}, *GetCurrencyQueries queries) returns Currency|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/currencies(${getEncodedUri(currencyId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a currency
    #
    # + companyId - The unique identifier of the company.
    # + currencyId - The unique identifier of the currency.
    # + headers - Headers to be sent with the request 
    # + return - The currency was deleted successfully. 
    remote isolated function deleteCurrency(string companyId, string currencyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/currencies(${getEncodedUri(currencyId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a currency
    #
    # + companyId - The unique identifier of the company.
    # + currencyId - The unique identifier of the currency.
    # + headers - Headers to be sent with the request 
    # + payload - The currency to create or update. 
    # + return - The updated currency was returned successfully. 
    remote isolated function updateCurrency(string companyId, string currencyId, UpdateCurrencyHeaders headers, CurrencyRequest payload) returns Currency|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/currencies(${getEncodedUri(currencyId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of payment methods
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of payment methods was returned successfully. 
    remote isolated function listPaymentMethods(string companyId, map<string|string[]> headers = {}, *ListPaymentMethodsQueries queries) returns PaymentMethodCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentMethods`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a payment method
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The payment method to create or update. 
    # + return - The payment method was created successfully. 
    remote isolated function createPaymentMethod(string companyId, PaymentMethodRequest payload, map<string|string[]> headers = {}) returns PaymentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentMethods`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a payment method
    #
    # + companyId - The unique identifier of the company.
    # + paymentMethodId - The unique identifier of the payment method.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested payment method was returned successfully. 
    remote isolated function getPaymentMethod(string companyId, string paymentMethodId, map<string|string[]> headers = {}, *GetPaymentMethodQueries queries) returns PaymentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentMethods(${getEncodedUri(paymentMethodId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a payment method
    #
    # + companyId - The unique identifier of the company.
    # + paymentMethodId - The unique identifier of the payment method.
    # + headers - Headers to be sent with the request 
    # + return - The payment method was deleted successfully. 
    remote isolated function deletePaymentMethod(string companyId, string paymentMethodId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentMethods(${getEncodedUri(paymentMethodId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a payment method
    #
    # + companyId - The unique identifier of the company.
    # + paymentMethodId - The unique identifier of the payment method.
    # + headers - Headers to be sent with the request 
    # + payload - The payment method to create or update. 
    # + return - The updated payment method was returned successfully. 
    remote isolated function updatePaymentMethod(string companyId, string paymentMethodId, UpdatePaymentMethodHeaders headers, PaymentMethodRequest payload) returns PaymentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentMethods(${getEncodedUri(paymentMethodId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of dimensions
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of dimensions was returned successfully. 
    remote isolated function listDimensions(string companyId, map<string|string[]> headers = {}, *ListDimensionsQueries queries) returns DimensionCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensions`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a dimension
    #
    # + companyId - The unique identifier of the company.
    # + dimensionId - The unique identifier of the dimension.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested dimension was returned successfully. 
    remote isolated function getDimension(string companyId, string dimensionId, map<string|string[]> headers = {}, *GetDimensionQueries queries) returns Dimension|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensions(${getEncodedUri(dimensionId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of dimension values for a dimension
    #
    # + companyId - The unique identifier of the company.
    # + dimensionId - The unique identifier of the dimension.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of dimension values was returned successfully. 
    remote isolated function listDimensionValuesForDimension(string companyId, string dimensionId, map<string|string[]> headers = {}, *ListDimensionValuesForDimensionQueries queries) returns DimensionValueCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensions(${getEncodedUri(dimensionId)})/dimensionValues`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a dimension value for a dimension
    #
    # + companyId - The unique identifier of the company.
    # + dimensionId - The unique identifier of the dimension.
    # + dimensionValueId - The unique identifier of the dimension value.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested dimension value was returned successfully. 
    remote isolated function getDimensionValueForDimension(string companyId, string dimensionId, string dimensionValueId, map<string|string[]> headers = {}, *GetDimensionValueForDimensionQueries queries) returns DimensionValue|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensions(${getEncodedUri(dimensionId)})/dimensionValues(${getEncodedUri(dimensionValueId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of dimension values
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of dimension values was returned successfully. 
    remote isolated function listDimensionValues(string companyId, map<string|string[]> headers = {}, *ListDimensionValuesQueries queries) returns DimensionValueCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionValues`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a dimension value
    #
    # + companyId - The unique identifier of the company.
    # + dimensionValueId - The unique identifier of the dimension value.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested dimension value was returned successfully. 
    remote isolated function getDimensionValue(string companyId, string dimensionValueId, map<string|string[]> headers = {}, *GetDimensionValueQueries queries) returns DimensionValue|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionValues(${getEncodedUri(dimensionValueId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of dimension lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of dimension lines was returned successfully. 
    remote isolated function listDimensionLines(string companyId, map<string|string[]> headers = {}, *ListDimensionLinesQueries queries) returns DimensionLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a dimension line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The dimension line to create or update. 
    # + return - The dimension line was created successfully. 
    remote isolated function createDimensionLine(string companyId, DimensionLineRequest payload, map<string|string[]> headers = {}) returns DimensionLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a dimension line
    #
    # + companyId - The unique identifier of the company.
    # + dimensionLineParentId - The parent id of the dimension line.
    # + dimensionLineId - The unique identifier of the dimension line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested dimension line was returned successfully. 
    remote isolated function getDimensionLine(string companyId, string dimensionLineParentId, string dimensionLineId, map<string|string[]> headers = {}, *GetDimensionLineQueries queries) returns DimensionLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionLines(${getEncodedUri(dimensionLineParentId)},${getEncodedUri(dimensionLineId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a dimension line
    #
    # + companyId - The unique identifier of the company.
    # + dimensionLineParentId - The parent id of the dimension line.
    # + dimensionLineId - The unique identifier of the dimension line.
    # + headers - Headers to be sent with the request 
    # + return - The dimension line was deleted successfully. 
    remote isolated function deleteDimensionLine(string companyId, string dimensionLineParentId, string dimensionLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionLines(${getEncodedUri(dimensionLineParentId)},${getEncodedUri(dimensionLineId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a dimension line
    #
    # + companyId - The unique identifier of the company.
    # + dimensionLineParentId - The parent id of the dimension line.
    # + dimensionLineId - The unique identifier of the dimension line.
    # + headers - Headers to be sent with the request 
    # + payload - The dimension line to create or update. 
    # + return - The updated dimension line was returned successfully. 
    remote isolated function updateDimensionLine(string companyId, string dimensionLineParentId, string dimensionLineId, UpdateDimensionLineHeaders headers, DimensionLineRequest payload) returns DimensionLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/dimensionLines(${getEncodedUri(dimensionLineParentId)},${getEncodedUri(dimensionLineId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of payment terms
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of payment terms was returned successfully. 
    remote isolated function listPaymentTerms(string companyId, map<string|string[]> headers = {}, *ListPaymentTermsQueries queries) returns PaymentTermCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentTerms`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a payment term
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The payment term to create or update. 
    # + return - The payment term was created successfully. 
    remote isolated function createPaymentTerm(string companyId, PaymentTermRequest payload, map<string|string[]> headers = {}) returns PaymentTerm|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentTerms`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a payment term
    #
    # + companyId - The unique identifier of the company.
    # + paymentTermId - The unique identifier of the payment term.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested payment term was returned successfully. 
    remote isolated function getPaymentTerm(string companyId, string paymentTermId, map<string|string[]> headers = {}, *GetPaymentTermQueries queries) returns PaymentTerm|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentTerms(${getEncodedUri(paymentTermId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a payment term
    #
    # + companyId - The unique identifier of the company.
    # + paymentTermId - The unique identifier of the payment term.
    # + headers - Headers to be sent with the request 
    # + return - The payment term was deleted successfully. 
    remote isolated function deletePaymentTerm(string companyId, string paymentTermId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentTerms(${getEncodedUri(paymentTermId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a payment term
    #
    # + companyId - The unique identifier of the company.
    # + paymentTermId - The unique identifier of the payment term.
    # + headers - Headers to be sent with the request 
    # + payload - The payment term to create or update. 
    # + return - The updated payment term was returned successfully. 
    remote isolated function updatePaymentTerm(string companyId, string paymentTermId, UpdatePaymentTermHeaders headers, PaymentTermRequest payload) returns PaymentTerm|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/paymentTerms(${getEncodedUri(paymentTermId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of shipment methods
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of shipment methods was returned successfully. 
    remote isolated function listShipmentMethods(string companyId, map<string|string[]> headers = {}, *ListShipmentMethodsQueries queries) returns ShipmentMethodCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/shipmentMethods`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a shipment method
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The shipment method to create or update. 
    # + return - The shipment method was created successfully. 
    remote isolated function createShipmentMethod(string companyId, ShipmentMethodRequest payload, map<string|string[]> headers = {}) returns ShipmentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/shipmentMethods`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a shipment method
    #
    # + companyId - The unique identifier of the company.
    # + shipmentMethodId - The unique identifier of the shipment method.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested shipment method was returned successfully. 
    remote isolated function getShipmentMethod(string companyId, string shipmentMethodId, map<string|string[]> headers = {}, *GetShipmentMethodQueries queries) returns ShipmentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/shipmentMethods(${getEncodedUri(shipmentMethodId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a shipment method
    #
    # + companyId - The unique identifier of the company.
    # + shipmentMethodId - The unique identifier of the shipment method.
    # + headers - Headers to be sent with the request 
    # + return - The shipment method was deleted successfully. 
    remote isolated function deleteShipmentMethod(string companyId, string shipmentMethodId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/shipmentMethods(${getEncodedUri(shipmentMethodId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a shipment method
    #
    # + companyId - The unique identifier of the company.
    # + shipmentMethodId - The unique identifier of the shipment method.
    # + headers - Headers to be sent with the request 
    # + payload - The shipment method to create or update. 
    # + return - The updated shipment method was returned successfully. 
    remote isolated function updateShipmentMethod(string companyId, string shipmentMethodId, UpdateShipmentMethodHeaders headers, ShipmentMethodRequest payload) returns ShipmentMethod|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/shipmentMethods(${getEncodedUri(shipmentMethodId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of item categories
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of item categories was returned successfully. 
    remote isolated function listItemCategories(string companyId, map<string|string[]> headers = {}, *ListItemCategoriesQueries queries) returns ItemCategoryCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/itemCategories`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an item category
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The item category to create or update. 
    # + return - The item category was created successfully. 
    remote isolated function createItemCategory(string companyId, ItemCategoryRequest payload, map<string|string[]> headers = {}) returns ItemCategory|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/itemCategories`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns an item category
    #
    # + companyId - The unique identifier of the company.
    # + itemCategoryId - The unique identifier of the item category.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested item category was returned successfully. 
    remote isolated function getItemCategory(string companyId, string itemCategoryId, map<string|string[]> headers = {}, *GetItemCategoryQueries queries) returns ItemCategory|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/itemCategories(${getEncodedUri(itemCategoryId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes an item category
    #
    # + companyId - The unique identifier of the company.
    # + itemCategoryId - The unique identifier of the item category.
    # + headers - Headers to be sent with the request 
    # + return - The item category was deleted successfully. 
    remote isolated function deleteItemCategory(string companyId, string itemCategoryId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/itemCategories(${getEncodedUri(itemCategoryId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates an item category
    #
    # + companyId - The unique identifier of the company.
    # + itemCategoryId - The unique identifier of the item category.
    # + headers - Headers to be sent with the request 
    # + payload - The item category to create or update. 
    # + return - The updated item category was returned successfully. 
    remote isolated function updateItemCategory(string companyId, string itemCategoryId, UpdateItemCategoryHeaders headers, ItemCategoryRequest payload) returns ItemCategory|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/itemCategories(${getEncodedUri(itemCategoryId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of cash flow statement lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of cash flow statement lines was returned successfully. 
    remote isolated function listCashFlowStatementLines(string companyId, map<string|string[]> headers = {}, *ListCashFlowStatementLinesQueries queries) returns CashFlowStatementCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/cashFlowStatement`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a cash flow statement line
    #
    # + companyId - The unique identifier of the company.
    # + cashFlowStatementLineNumber - The line number of the cash flow statement.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested cash flow statement line was returned successfully. 
    remote isolated function getCashFlowStatementLine(string companyId, int:Signed32 cashFlowStatementLineNumber, map<string|string[]> headers = {}, *GetCashFlowStatementLineQueries queries) returns CashFlowStatement|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/cashFlowStatement(${getEncodedUri(cashFlowStatementLineNumber)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of countries/regions
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of countries/regions was returned successfully. 
    remote isolated function listCountriesRegions(string companyId, map<string|string[]> headers = {}, *ListCountriesRegionsQueries queries) returns CountryRegionCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/countriesRegions`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a country/region
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The country region to create or update. 
    # + return - The country/region was created successfully. 
    remote isolated function createCountryRegion(string companyId, CountryRegionRequest payload, map<string|string[]> headers = {}) returns CountryRegion|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/countriesRegions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a country/region
    #
    # + companyId - The unique identifier of the company.
    # + countryRegionId - The unique identifier of the country region.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested country/region was returned successfully. 
    remote isolated function getCountryRegion(string companyId, string countryRegionId, map<string|string[]> headers = {}, *GetCountryRegionQueries queries) returns CountryRegion|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/countriesRegions(${getEncodedUri(countryRegionId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a country/region
    #
    # + companyId - The unique identifier of the company.
    # + countryRegionId - The unique identifier of the country region.
    # + headers - Headers to be sent with the request 
    # + return - The country/region was deleted successfully. 
    remote isolated function deleteCountryRegion(string companyId, string countryRegionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/countriesRegions(${getEncodedUri(countryRegionId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a country/region
    #
    # + companyId - The unique identifier of the company.
    # + countryRegionId - The unique identifier of the country region.
    # + headers - Headers to be sent with the request 
    # + payload - The country region to create or update. 
    # + return - The updated country/region was returned successfully. 
    remote isolated function updateCountryRegion(string companyId, string countryRegionId, UpdateCountryRegionHeaders headers, CountryRegionRequest payload) returns CountryRegion|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/countriesRegions(${getEncodedUri(countryRegionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of sales orders
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales orders was returned successfully. 
    remote isolated function listSalesOrders(string companyId, map<string|string[]> headers = {}, *ListSalesOrdersQueries queries) returns SalesOrderCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales order
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order to create or update. 
    # + return - The sales order was created successfully. 
    remote isolated function createSalesOrder(string companyId, SalesOrderRequest payload, map<string|string[]> headers = {}) returns SalesOrder|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales order was returned successfully. 
    remote isolated function getSalesOrder(string companyId, string salesOrderId, map<string|string[]> headers = {}, *GetSalesOrderQueries queries) returns SalesOrder|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + return - The sales order was deleted successfully. 
    remote isolated function deleteSalesOrder(string companyId, string salesOrderId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order to create or update. 
    # + return - The updated sales order was returned successfully. 
    remote isolated function updateSalesOrder(string companyId, string salesOrderId, UpdateSalesOrderHeaders headers, SalesOrderRequest payload) returns SalesOrder|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the ship and invoice action on a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + return - The ship and invoice action completed successfully. 
    remote isolated function shipAndInvoiceSalesOrder(string companyId, string salesOrderId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/Microsoft.NAV.shipAndInvoice`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of sales order lines for a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales order lines was returned successfully. 
    remote isolated function listSalesOrderLinesForSalesOrder(string companyId, string salesOrderId, map<string|string[]> headers = {}, *ListSalesOrderLinesForSalesOrderQueries queries) returns SalesOrderLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/salesOrderLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales order line for a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order line to create or update. 
    # + return - The sales order line was created successfully. 
    remote isolated function createSalesOrderLineForSalesOrder(string companyId, string salesOrderId, SalesOrderLineRequest payload, map<string|string[]> headers = {}) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/salesOrderLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales order line for a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales order line was returned successfully. 
    remote isolated function getSalesOrderLineForSalesOrder(string companyId, string salesOrderId, string salesOrderLineId, map<string|string[]> headers = {}, *GetSalesOrderLineForSalesOrderQueries queries) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales order line for a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + return - The sales order line was deleted successfully. 
    remote isolated function deleteSalesOrderLineForSalesOrder(string companyId, string salesOrderId, string salesOrderLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales order line for a sales order
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderId - The unique identifier of the sales order.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order line to create or update. 
    # + return - The updated sales order line was returned successfully. 
    remote isolated function updateSalesOrderLineForSalesOrder(string companyId, string salesOrderId, string salesOrderLineId, UpdateSalesOrderLineForSalesOrderHeaders headers, SalesOrderLineRequest payload) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrders(${getEncodedUri(salesOrderId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of sales order lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales order lines was returned successfully. 
    remote isolated function listSalesOrderLines(string companyId, map<string|string[]> headers = {}, *ListSalesOrderLinesQueries queries) returns SalesOrderLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrderLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales order line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order line to create or update. 
    # + return - The sales order line was created successfully. 
    remote isolated function createSalesOrderLine(string companyId, SalesOrderLineRequest payload, map<string|string[]> headers = {}) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrderLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales order line
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales order line was returned successfully. 
    remote isolated function getSalesOrderLine(string companyId, string salesOrderLineId, map<string|string[]> headers = {}, *GetSalesOrderLineQueries queries) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales order line
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + return - The sales order line was deleted successfully. 
    remote isolated function deleteSalesOrderLine(string companyId, string salesOrderLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales order line
    #
    # + companyId - The unique identifier of the company.
    # + salesOrderLineId - The unique identifier of the sales order line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales order line to create or update. 
    # + return - The updated sales order line was returned successfully. 
    remote isolated function updateSalesOrderLine(string companyId, string salesOrderLineId, UpdateSalesOrderLineHeaders headers, SalesOrderLineRequest payload) returns SalesOrderLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesOrderLines('${getEncodedUri(salesOrderLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of retained earnings statement lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of retained earnings statement lines was returned successfully. 
    remote isolated function listRetainedEarningsStatementLines(string companyId, map<string|string[]> headers = {}, *ListRetainedEarningsStatementLinesQueries queries) returns RetainedEarningsStatementCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/retainedEarningsStatement`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a retained earnings statement line
    #
    # + companyId - The unique identifier of the company.
    # + retainedEarningsStatementLineNumber - The line number of the retained earnings statement.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested retained earnings statement line was returned successfully. 
    remote isolated function getRetainedEarningsStatementLine(string companyId, int:Signed32 retainedEarningsStatementLineNumber, map<string|string[]> headers = {}, *GetRetainedEarningsStatementLineQueries queries) returns RetainedEarningsStatement|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/retainedEarningsStatement(${getEncodedUri(retainedEarningsStatementLineNumber)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of units of measure
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of units of measure was returned successfully. 
    remote isolated function listUnitsOfMeasure(string companyId, map<string|string[]> headers = {}, *ListUnitsOfMeasureQueries queries) returns UnitOfMeasureCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/unitsOfMeasure`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a unit of measure
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The unit of measure to create or update. 
    # + return - The unit of measure was created successfully. 
    remote isolated function createUnitOfMeasure(string companyId, UnitOfMeasureRequest payload, map<string|string[]> headers = {}) returns UnitOfMeasure|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/unitsOfMeasure`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a unit of measure
    #
    # + companyId - The unique identifier of the company.
    # + unitOfMeasureId - The unique identifier of the unit of measure.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested unit of measure was returned successfully. 
    remote isolated function getUnitOfMeasure(string companyId, string unitOfMeasureId, map<string|string[]> headers = {}, *GetUnitOfMeasureQueries queries) returns UnitOfMeasure|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/unitsOfMeasure(${getEncodedUri(unitOfMeasureId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a unit of measure
    #
    # + companyId - The unique identifier of the company.
    # + unitOfMeasureId - The unique identifier of the unit of measure.
    # + headers - Headers to be sent with the request 
    # + return - The unit of measure was deleted successfully. 
    remote isolated function deleteUnitOfMeasure(string companyId, string unitOfMeasureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/unitsOfMeasure(${getEncodedUri(unitOfMeasureId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a unit of measure
    #
    # + companyId - The unique identifier of the company.
    # + unitOfMeasureId - The unique identifier of the unit of measure.
    # + headers - Headers to be sent with the request 
    # + payload - The unit of measure to create or update. 
    # + return - The updated unit of measure was returned successfully. 
    remote isolated function updateUnitOfMeasure(string companyId, string unitOfMeasureId, UpdateUnitOfMeasureHeaders headers, UnitOfMeasureRequest payload) returns UnitOfMeasure|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/unitsOfMeasure(${getEncodedUri(unitOfMeasureId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns the aged accounts receivable report
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The aged accounts receivable report was returned successfully. 
    remote isolated function listAgedAccountsReceivable(string companyId, map<string|string[]> headers = {}, *ListAgedAccountsReceivableQueries queries) returns AgedAccountsReceivableCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/agedAccountsReceivable`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns the aged accounts receivable for a customer
    #
    # + companyId - The unique identifier of the company.
    # + agedAccountsReceivableCustomerId - The customer id of the aged accounts receivable.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The aged accounts receivable for the customer was returned successfully. 
    remote isolated function getAgedAccountsReceivableByCustomer(string companyId, string agedAccountsReceivableCustomerId, map<string|string[]> headers = {}, *GetAgedAccountsReceivableByCustomerQueries queries) returns AgedAccountsReceivable|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/agedAccountsReceivable(${getEncodedUri(agedAccountsReceivableCustomerId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns the aged accounts payable report
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The aged accounts payable report was returned successfully. 
    remote isolated function listAgedAccountsPayable(string companyId, map<string|string[]> headers = {}, *ListAgedAccountsPayableQueries queries) returns AgedAccountsPayableCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/agedAccountsPayable`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns the aged accounts payable for a vendor
    #
    # + companyId - The unique identifier of the company.
    # + agedAccountsPayableVendorId - The vendor id of the aged accounts payable.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The aged accounts payable for the vendor was returned successfully. 
    remote isolated function getAgedAccountsPayableByVendor(string companyId, string agedAccountsPayableVendorId, map<string|string[]> headers = {}, *GetAgedAccountsPayableByVendorQueries queries) returns AgedAccountsPayable|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/agedAccountsPayable(${getEncodedUri(agedAccountsPayableVendorId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of balance sheet lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of balance sheet lines was returned successfully. 
    remote isolated function listBalanceSheetLines(string companyId, map<string|string[]> headers = {}, *ListBalanceSheetLinesQueries queries) returns BalanceSheetCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/balanceSheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a balance sheet line
    #
    # + companyId - The unique identifier of the company.
    # + balanceSheetLineNumber - The line number of the balance sheet.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested balance sheet line was returned successfully. 
    remote isolated function getBalanceSheetLine(string companyId, int:Signed32 balanceSheetLineNumber, map<string|string[]> headers = {}, *GetBalanceSheetLineQueries queries) returns BalanceSheet|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/balanceSheet(${getEncodedUri(balanceSheetLineNumber)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of trial balance lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of trial balance lines was returned successfully. 
    remote isolated function listTrialBalanceLines(string companyId, map<string|string[]> headers = {}, *ListTrialBalanceLinesQueries queries) returns TrialBalanceCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/trialBalance`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a trial balance line
    #
    # + companyId - The unique identifier of the company.
    # + trialBalanceNumber - The number of the trial balance.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested trial balance line was returned successfully. 
    remote isolated function getTrialBalanceLine(string companyId, string trialBalanceNumber, map<string|string[]> headers = {}, *GetTrialBalanceLineQueries queries) returns TrialBalance|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/trialBalance('${getEncodedUri(trialBalanceNumber)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of income statement lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of income statement lines was returned successfully. 
    remote isolated function listIncomeStatementLines(string companyId, map<string|string[]> headers = {}, *ListIncomeStatementLinesQueries queries) returns IncomeStatementCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/incomeStatement`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns an income statement line
    #
    # + companyId - The unique identifier of the company.
    # + incomeStatementLineNumber - The line number of the income statement.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested income statement line was returned successfully. 
    remote isolated function getIncomeStatementLine(string companyId, int:Signed32 incomeStatementLineNumber, map<string|string[]> headers = {}, *GetIncomeStatementLineQueries queries) returns IncomeStatement|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/incomeStatement(${getEncodedUri(incomeStatementLineNumber)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of tax areas
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of tax areas was returned successfully. 
    remote isolated function listTaxAreas(string companyId, map<string|string[]> headers = {}, *ListTaxAreasQueries queries) returns TaxAreaCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxAreas`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a tax area
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The tax area to create or update. 
    # + return - The tax area was created successfully. 
    remote isolated function createTaxArea(string companyId, TaxAreaRequest payload, map<string|string[]> headers = {}) returns TaxArea|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxAreas`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a tax area
    #
    # + companyId - The unique identifier of the company.
    # + taxAreaId - The unique identifier of the tax area.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested tax area was returned successfully. 
    remote isolated function getTaxArea(string companyId, string taxAreaId, map<string|string[]> headers = {}, *GetTaxAreaQueries queries) returns TaxArea|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxAreas(${getEncodedUri(taxAreaId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a tax area
    #
    # + companyId - The unique identifier of the company.
    # + taxAreaId - The unique identifier of the tax area.
    # + headers - Headers to be sent with the request 
    # + return - The tax area was deleted successfully. 
    remote isolated function deleteTaxArea(string companyId, string taxAreaId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxAreas(${getEncodedUri(taxAreaId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a tax area
    #
    # + companyId - The unique identifier of the company.
    # + taxAreaId - The unique identifier of the tax area.
    # + headers - Headers to be sent with the request 
    # + payload - The tax area to create or update. 
    # + return - The updated tax area was returned successfully. 
    remote isolated function updateTaxArea(string companyId, string taxAreaId, UpdateTaxAreaHeaders headers, TaxAreaRequest payload) returns TaxArea|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/taxAreas(${getEncodedUri(taxAreaId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of sales quotes
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales quotes was returned successfully. 
    remote isolated function listSalesQuotes(string companyId, map<string|string[]> headers = {}, *ListSalesQuotesQueries queries) returns SalesQuoteCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote to create or update. 
    # + return - The sales quote was created successfully. 
    remote isolated function createSalesQuote(string companyId, SalesQuoteRequest payload, map<string|string[]> headers = {}) returns SalesQuote|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales quote was returned successfully. 
    remote isolated function getSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}, *GetSalesQuoteQueries queries) returns SalesQuote|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + return - The sales quote was deleted successfully. 
    remote isolated function deleteSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote to create or update. 
    # + return - The updated sales quote was returned successfully. 
    remote isolated function updateSalesQuote(string companyId, string salesQuoteId, UpdateSalesQuoteHeaders headers, SalesQuoteRequest payload) returns SalesQuote|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the make invoice action on a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + return - The make invoice action completed successfully. 
    remote isolated function makeInvoiceFromSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/Microsoft.NAV.makeInvoice`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the make order action on a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + return - The make order action completed successfully. 
    remote isolated function makeOrderFromSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/Microsoft.NAV.makeOrder`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the send action on a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + return - The send action completed successfully. 
    remote isolated function sendSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/Microsoft.NAV.send`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of sales quote lines for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales quote lines was returned successfully. 
    remote isolated function listSalesQuoteLinesForSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}, *ListSalesQuoteLinesForSalesQuoteQueries queries) returns SalesQuoteLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/salesQuoteLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales quote line for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote line to create or update. 
    # + return - The sales quote line was created successfully. 
    remote isolated function createSalesQuoteLineForSalesQuote(string companyId, string salesQuoteId, SalesQuoteLineRequest payload, map<string|string[]> headers = {}) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/salesQuoteLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales quote line for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales quote line was returned successfully. 
    remote isolated function getSalesQuoteLineForSalesQuote(string companyId, string salesQuoteId, string salesQuoteLineId, map<string|string[]> headers = {}, *GetSalesQuoteLineForSalesQuoteQueries queries) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales quote line for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + return - The sales quote line was deleted successfully. 
    remote isolated function deleteSalesQuoteLineForSalesQuote(string companyId, string salesQuoteId, string salesQuoteLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales quote line for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote line to create or update. 
    # + return - The updated sales quote line was returned successfully. 
    remote isolated function updateSalesQuoteLineForSalesQuote(string companyId, string salesQuoteId, string salesQuoteLineId, UpdateSalesQuoteLineForSalesQuoteHeaders headers, SalesQuoteLineRequest payload) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of PDF documents for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of PDF documents was returned successfully. 
    remote isolated function listPdfDocumentsForSalesQuote(string companyId, string salesQuoteId, map<string|string[]> headers = {}, *ListPdfDocumentsForSalesQuoteQueries queries) returns PdfDocumentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/pdfDocument`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a PDF document for a sales quote
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteId - The unique identifier of the sales quote.
    # + pdfDocumentId - The unique identifier of the pdf document.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested PDF document was returned successfully. 
    remote isolated function getPdfDocumentForSalesQuote(string companyId, string salesQuoteId, string pdfDocumentId, map<string|string[]> headers = {}, *GetPdfDocumentForSalesQuoteQueries queries) returns PdfDocument|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuotes(${getEncodedUri(salesQuoteId)})/pdfDocument(${getEncodedUri(pdfDocumentId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of sales quote lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales quote lines was returned successfully. 
    remote isolated function listSalesQuoteLines(string companyId, map<string|string[]> headers = {}, *ListSalesQuoteLinesQueries queries) returns SalesQuoteLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuoteLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales quote line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote line to create or update. 
    # + return - The sales quote line was created successfully. 
    remote isolated function createSalesQuoteLine(string companyId, SalesQuoteLineRequest payload, map<string|string[]> headers = {}) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuoteLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales quote line
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales quote line was returned successfully. 
    remote isolated function getSalesQuoteLine(string companyId, string salesQuoteLineId, map<string|string[]> headers = {}, *GetSalesQuoteLineQueries queries) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales quote line
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + return - The sales quote line was deleted successfully. 
    remote isolated function deleteSalesQuoteLine(string companyId, string salesQuoteLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales quote line
    #
    # + companyId - The unique identifier of the company.
    # + salesQuoteLineId - The unique identifier of the sales quote line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales quote line to create or update. 
    # + return - The updated sales quote line was returned successfully. 
    remote isolated function updateSalesQuoteLine(string companyId, string salesQuoteLineId, UpdateSalesQuoteLineHeaders headers, SalesQuoteLineRequest payload) returns SalesQuoteLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesQuoteLines('${getEncodedUri(salesQuoteLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of sales credit memos
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales credit memos was returned successfully. 
    remote isolated function listSalesCreditMemos(string companyId, map<string|string[]> headers = {}, *ListSalesCreditMemosQueries queries) returns SalesCreditMemoCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo to create or update. 
    # + return - The sales credit memo was created successfully. 
    remote isolated function createSalesCreditMemo(string companyId, SalesCreditMemoRequest payload, map<string|string[]> headers = {}) returns SalesCreditMemo|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales credit memo was returned successfully. 
    remote isolated function getSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}, *GetSalesCreditMemoQueries queries) returns SalesCreditMemo|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The sales credit memo was deleted successfully. 
    remote isolated function deleteSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo to create or update. 
    # + return - The updated sales credit memo was returned successfully. 
    remote isolated function updateSalesCreditMemo(string companyId, string salesCreditMemoId, UpdateSalesCreditMemoHeaders headers, SalesCreditMemoRequest payload) returns SalesCreditMemo|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the cancel and send action on a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The cancel and send action completed successfully. 
    remote isolated function cancelAndSendSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/Microsoft.NAV.cancelAndSend`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the cancel action on a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The cancel action completed successfully. 
    remote isolated function cancelSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/Microsoft.NAV.cancel`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the post and send action on a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The post and send action completed successfully. 
    remote isolated function postAndSendSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/Microsoft.NAV.postAndSend`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the post action on a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The post action completed successfully. 
    remote isolated function postSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/Microsoft.NAV.post`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Performs the send action on a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + return - The send action completed successfully. 
    remote isolated function sendSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/Microsoft.NAV.send`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of sales credit memo lines for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales credit memo lines was returned successfully. 
    remote isolated function listSalesCreditMemoLinesForSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}, *ListSalesCreditMemoLinesForSalesCreditMemoQueries queries) returns SalesCreditMemoLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/salesCreditMemoLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales credit memo line for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo line to create or update. 
    # + return - The sales credit memo line was created successfully. 
    remote isolated function createSalesCreditMemoLineForSalesCreditMemo(string companyId, string salesCreditMemoId, SalesCreditMemoLineRequest payload, map<string|string[]> headers = {}) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/salesCreditMemoLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales credit memo line for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales credit memo line was returned successfully. 
    remote isolated function getSalesCreditMemoLineForSalesCreditMemo(string companyId, string salesCreditMemoId, string salesCreditMemoLineId, map<string|string[]> headers = {}, *GetSalesCreditMemoLineForSalesCreditMemoQueries queries) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales credit memo line for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + return - The sales credit memo line was deleted successfully. 
    remote isolated function deleteSalesCreditMemoLineForSalesCreditMemo(string companyId, string salesCreditMemoId, string salesCreditMemoLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales credit memo line for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo line to create or update. 
    # + return - The updated sales credit memo line was returned successfully. 
    remote isolated function updateSalesCreditMemoLineForSalesCreditMemo(string companyId, string salesCreditMemoId, string salesCreditMemoLineId, UpdateSalesCreditMemoLineForSalesCreditMemoHeaders headers, SalesCreditMemoLineRequest payload) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of PDF documents for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of PDF documents was returned successfully. 
    remote isolated function listPdfDocumentsForSalesCreditMemo(string companyId, string salesCreditMemoId, map<string|string[]> headers = {}, *ListPdfDocumentsForSalesCreditMemoQueries queries) returns PdfDocumentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/pdfDocument`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a PDF document for a sales credit memo
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoId - The unique identifier of the sales credit memo.
    # + pdfDocumentId - The unique identifier of the pdf document.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested PDF document was returned successfully. 
    remote isolated function getPdfDocumentForSalesCreditMemo(string companyId, string salesCreditMemoId, string pdfDocumentId, map<string|string[]> headers = {}, *GetPdfDocumentForSalesCreditMemoQueries queries) returns PdfDocument|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemos(${getEncodedUri(salesCreditMemoId)})/pdfDocument(${getEncodedUri(pdfDocumentId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of sales credit memo lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of sales credit memo lines was returned successfully. 
    remote isolated function listSalesCreditMemoLines(string companyId, map<string|string[]> headers = {}, *ListSalesCreditMemoLinesQueries queries) returns SalesCreditMemoLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemoLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a sales credit memo line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo line to create or update. 
    # + return - The sales credit memo line was created successfully. 
    remote isolated function createSalesCreditMemoLine(string companyId, SalesCreditMemoLineRequest payload, map<string|string[]> headers = {}) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemoLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a sales credit memo line
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested sales credit memo line was returned successfully. 
    remote isolated function getSalesCreditMemoLine(string companyId, string salesCreditMemoLineId, map<string|string[]> headers = {}, *GetSalesCreditMemoLineQueries queries) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a sales credit memo line
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + return - The sales credit memo line was deleted successfully. 
    remote isolated function deleteSalesCreditMemoLine(string companyId, string salesCreditMemoLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a sales credit memo line
    #
    # + companyId - The unique identifier of the company.
    # + salesCreditMemoLineId - The unique identifier of the sales credit memo line.
    # + headers - Headers to be sent with the request 
    # + payload - The sales credit memo line to create or update. 
    # + return - The updated sales credit memo line was returned successfully. 
    remote isolated function updateSalesCreditMemoLine(string companyId, string salesCreditMemoLineId, UpdateSalesCreditMemoLineHeaders headers, SalesCreditMemoLineRequest payload) returns SalesCreditMemoLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/salesCreditMemoLines('${getEncodedUri(salesCreditMemoLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of general ledger entry attachments
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of general ledger entry attachments was returned successfully. 
    remote isolated function listGeneralLedgerEntryAttachments(string companyId, map<string|string[]> headers = {}, *ListGeneralLedgerEntryAttachmentsQueries queries) returns GeneralLedgerEntryAttachmentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntryAttachments`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a general ledger entry attachment
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The general ledger entry attachments to create or update. 
    # + return - The general ledger entry attachment was created successfully. 
    remote isolated function createGeneralLedgerEntryAttachment(string companyId, GeneralLedgerEntryAttachmentsRequest payload, map<string|string[]> headers = {}) returns GeneralLedgerEntryAttachments|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntryAttachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a general ledger entry attachment
    #
    # + companyId - The unique identifier of the company.
    # + generalLedgerEntryAttachmentsGeneralLedgerEntryNumber - The general ledger entry number of the general ledger entry attachments.
    # + generalLedgerEntryAttachmentsId - The unique identifier of the general ledger entry attachments.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested general ledger entry attachment was returned successfully. 
    remote isolated function getGeneralLedgerEntryAttachment(string companyId, int:Signed32 generalLedgerEntryAttachmentsGeneralLedgerEntryNumber, string generalLedgerEntryAttachmentsId, map<string|string[]> headers = {}, *GetGeneralLedgerEntryAttachmentQueries queries) returns GeneralLedgerEntryAttachments|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntryAttachments(${getEncodedUri(generalLedgerEntryAttachmentsGeneralLedgerEntryNumber)},${getEncodedUri(generalLedgerEntryAttachmentsId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a general ledger entry attachment
    #
    # + companyId - The unique identifier of the company.
    # + generalLedgerEntryAttachmentsGeneralLedgerEntryNumber - The general ledger entry number of the general ledger entry attachments.
    # + generalLedgerEntryAttachmentsId - The unique identifier of the general ledger entry attachments.
    # + headers - Headers to be sent with the request 
    # + return - The general ledger entry attachment was deleted successfully. 
    remote isolated function deleteGeneralLedgerEntryAttachment(string companyId, int:Signed32 generalLedgerEntryAttachmentsGeneralLedgerEntryNumber, string generalLedgerEntryAttachmentsId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntryAttachments(${getEncodedUri(generalLedgerEntryAttachmentsGeneralLedgerEntryNumber)},${getEncodedUri(generalLedgerEntryAttachmentsId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a general ledger entry attachment
    #
    # + companyId - The unique identifier of the company.
    # + generalLedgerEntryAttachmentsGeneralLedgerEntryNumber - The general ledger entry number of the general ledger entry attachments.
    # + generalLedgerEntryAttachmentsId - The unique identifier of the general ledger entry attachments.
    # + headers - Headers to be sent with the request 
    # + payload - The general ledger entry attachments to create or update. 
    # + return - The updated general ledger entry attachment was returned successfully. 
    remote isolated function updateGeneralLedgerEntryAttachment(string companyId, int:Signed32 generalLedgerEntryAttachmentsGeneralLedgerEntryNumber, string generalLedgerEntryAttachmentsId, UpdateGeneralLedgerEntryAttachmentHeaders headers, GeneralLedgerEntryAttachmentsRequest payload) returns GeneralLedgerEntryAttachments|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/generalLedgerEntryAttachments(${getEncodedUri(generalLedgerEntryAttachmentsGeneralLedgerEntryNumber)},${getEncodedUri(generalLedgerEntryAttachmentsId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of purchase invoices
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of purchase invoices was returned successfully. 
    remote isolated function listPurchaseInvoices(string companyId, map<string|string[]> headers = {}, *ListPurchaseInvoicesQueries queries) returns PurchaseInvoiceCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice to create or update. 
    # + return - The purchase invoice was created successfully. 
    remote isolated function createPurchaseInvoice(string companyId, PurchaseInvoiceRequest payload, map<string|string[]> headers = {}) returns PurchaseInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested purchase invoice was returned successfully. 
    remote isolated function getPurchaseInvoice(string companyId, string purchaseInvoiceId, map<string|string[]> headers = {}, *GetPurchaseInvoiceQueries queries) returns PurchaseInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + return - The purchase invoice was deleted successfully. 
    remote isolated function deletePurchaseInvoice(string companyId, string purchaseInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice to create or update. 
    # + return - The updated purchase invoice was returned successfully. 
    remote isolated function updatePurchaseInvoice(string companyId, string purchaseInvoiceId, UpdatePurchaseInvoiceHeaders headers, PurchaseInvoiceRequest payload) returns PurchaseInvoice|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Performs the post action on a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + return - The post action completed successfully. 
    remote isolated function postPurchaseInvoice(string companyId, string purchaseInvoiceId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/Microsoft.NAV.post`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a list of purchase invoice lines for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of purchase invoice lines was returned successfully. 
    remote isolated function listPurchaseInvoiceLinesForPurchaseInvoice(string companyId, string purchaseInvoiceId, map<string|string[]> headers = {}, *ListPurchaseInvoiceLinesForPurchaseInvoiceQueries queries) returns PurchaseInvoiceLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/purchaseInvoiceLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a purchase invoice line for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice line to create or update. 
    # + return - The purchase invoice line was created successfully. 
    remote isolated function createPurchaseInvoiceLineForPurchaseInvoice(string companyId, string purchaseInvoiceId, PurchaseInvoiceLineRequest payload, map<string|string[]> headers = {}) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/purchaseInvoiceLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a purchase invoice line for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested purchase invoice line was returned successfully. 
    remote isolated function getPurchaseInvoiceLineForPurchaseInvoice(string companyId, string purchaseInvoiceId, string purchaseInvoiceLineId, map<string|string[]> headers = {}, *GetPurchaseInvoiceLineForPurchaseInvoiceQueries queries) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a purchase invoice line for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + return - The purchase invoice line was deleted successfully. 
    remote isolated function deletePurchaseInvoiceLineForPurchaseInvoice(string companyId, string purchaseInvoiceId, string purchaseInvoiceLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a purchase invoice line for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice line to create or update. 
    # + return - The updated purchase invoice line was returned successfully. 
    remote isolated function updatePurchaseInvoiceLineForPurchaseInvoice(string companyId, string purchaseInvoiceId, string purchaseInvoiceLineId, UpdatePurchaseInvoiceLineForPurchaseInvoiceHeaders headers, PurchaseInvoiceLineRequest payload) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of PDF documents for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of PDF documents was returned successfully. 
    remote isolated function listPdfDocumentsForPurchaseInvoice(string companyId, string purchaseInvoiceId, map<string|string[]> headers = {}, *ListPdfDocumentsForPurchaseInvoiceQueries queries) returns PdfDocumentCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/pdfDocument`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a PDF document for a purchase invoice
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceId - The unique identifier of the purchase invoice.
    # + pdfDocumentId - The unique identifier of the pdf document.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested PDF document was returned successfully. 
    remote isolated function getPdfDocumentForPurchaseInvoice(string companyId, string purchaseInvoiceId, string pdfDocumentId, map<string|string[]> headers = {}, *GetPdfDocumentForPurchaseInvoiceQueries queries) returns PdfDocument|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoices(${getEncodedUri(purchaseInvoiceId)})/pdfDocument(${getEncodedUri(pdfDocumentId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of purchase invoice lines
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of purchase invoice lines was returned successfully. 
    remote isolated function listPurchaseInvoiceLines(string companyId, map<string|string[]> headers = {}, *ListPurchaseInvoiceLinesQueries queries) returns PurchaseInvoiceLineCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoiceLines`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a purchase invoice line
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice line to create or update. 
    # + return - The purchase invoice line was created successfully. 
    remote isolated function createPurchaseInvoiceLine(string companyId, PurchaseInvoiceLineRequest payload, map<string|string[]> headers = {}) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoiceLines`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a purchase invoice line
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested purchase invoice line was returned successfully. 
    remote isolated function getPurchaseInvoiceLine(string companyId, string purchaseInvoiceLineId, map<string|string[]> headers = {}, *GetPurchaseInvoiceLineQueries queries) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        map<Encoding> queryParamEncoding = {"$expand": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a purchase invoice line
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + return - The purchase invoice line was deleted successfully. 
    remote isolated function deletePurchaseInvoiceLine(string companyId, string purchaseInvoiceLineId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a purchase invoice line
    #
    # + companyId - The unique identifier of the company.
    # + purchaseInvoiceLineId - The unique identifier of the purchase invoice line.
    # + headers - Headers to be sent with the request 
    # + payload - The purchase invoice line to create or update. 
    # + return - The updated purchase invoice line was returned successfully. 
    remote isolated function updatePurchaseInvoiceLine(string companyId, string purchaseInvoiceLineId, UpdatePurchaseInvoiceLineHeaders headers, PurchaseInvoiceLineRequest payload) returns PurchaseInvoiceLine|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/purchaseInvoiceLines('${getEncodedUri(purchaseInvoiceLineId)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of projects
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of projects was returned successfully. 
    remote isolated function listProjects(string companyId, map<string|string[]> headers = {}, *ListProjectsQueries queries) returns ProjectCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/projects`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a project
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The project to create or update. 
    # + return - The project was created successfully. 
    remote isolated function createProject(string companyId, ProjectRequest payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/projects`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a project
    #
    # + companyId - The unique identifier of the company.
    # + projectId - The unique identifier of the project.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested project was returned successfully. 
    remote isolated function getProject(string companyId, string projectId, map<string|string[]> headers = {}, *GetProjectQueries queries) returns Project|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/projects(${getEncodedUri(projectId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a project
    #
    # + companyId - The unique identifier of the company.
    # + projectId - The unique identifier of the project.
    # + headers - Headers to be sent with the request 
    # + return - The project was deleted successfully. 
    remote isolated function deleteProject(string companyId, string projectId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/projects(${getEncodedUri(projectId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a project
    #
    # + companyId - The unique identifier of the company.
    # + projectId - The unique identifier of the project.
    # + headers - Headers to be sent with the request 
    # + payload - The project to create or update. 
    # + return - The updated project was returned successfully. 
    remote isolated function updateProject(string companyId, string projectId, UpdateProjectHeaders headers, ProjectRequest payload) returns Project|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/projects(${getEncodedUri(projectId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of bank accounts
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of bank accounts was returned successfully. 
    remote isolated function listBankAccounts(string companyId, map<string|string[]> headers = {}, *ListBankAccountsQueries queries) returns BankAccountCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/bankAccounts`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a bank account
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + payload - The bank account to create or update. 
    # + return - The bank account was created successfully. 
    remote isolated function createBankAccount(string companyId, BankAccountRequest payload, map<string|string[]> headers = {}) returns BankAccount|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/bankAccounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a bank account
    #
    # + companyId - The unique identifier of the company.
    # + bankAccountId - The unique identifier of the bank account.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested bank account was returned successfully. 
    remote isolated function getBankAccount(string companyId, string bankAccountId, map<string|string[]> headers = {}, *GetBankAccountQueries queries) returns BankAccount|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/bankAccounts(${getEncodedUri(bankAccountId)})`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a bank account
    #
    # + companyId - The unique identifier of the company.
    # + bankAccountId - The unique identifier of the bank account.
    # + headers - Headers to be sent with the request 
    # + return - The bank account was deleted successfully. 
    remote isolated function deleteBankAccount(string companyId, string bankAccountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/bankAccounts(${getEncodedUri(bankAccountId)})`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates a bank account
    #
    # + companyId - The unique identifier of the company.
    # + bankAccountId - The unique identifier of the bank account.
    # + headers - Headers to be sent with the request 
    # + payload - The bank account to create or update. 
    # + return - The updated bank account was returned successfully. 
    remote isolated function updateBankAccount(string companyId, string bankAccountId, UpdateBankAccountHeaders headers, BankAccountRequest payload) returns BankAccount|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/bankAccounts(${getEncodedUri(bankAccountId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Returns a list of customer sales records
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of customer sales records was returned successfully. 
    remote isolated function listCustomerSales(string companyId, map<string|string[]> headers = {}, *ListCustomerSalesQueries queries) returns CustomerSaleCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerSales`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a customer sales record
    #
    # + companyId - The unique identifier of the company.
    # + customerSaleCustomerId - The customer id of the customer sale.
    # + customerSaleCustomerNumber - The customer number of the customer sale.
    # + customerSaleName - The name of the customer sale.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested customer sales record was returned successfully. 
    remote isolated function getCustomerSale(string companyId, string customerSaleCustomerId, string customerSaleCustomerNumber, string customerSaleName, map<string|string[]> headers = {}, *GetCustomerSaleQueries queries) returns CustomerSale|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/customerSales(${getEncodedUri(customerSaleCustomerId)},'${getEncodedUri(customerSaleCustomerNumber)}','${getEncodedUri(customerSaleName)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of vendor purchase records
    #
    # + companyId - The unique identifier of the company.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A list of vendor purchase records was returned successfully. 
    remote isolated function listVendorPurchases(string companyId, map<string|string[]> headers = {}, *ListVendorPurchasesQueries queries) returns VendorPurchaseCollection|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendorPurchases`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a vendor purchase record
    #
    # + companyId - The unique identifier of the company.
    # + vendorPurchaseVendorId - The vendor id of the vendor purchase.
    # + vendorPurchaseVendorNumber - The vendor number of the vendor purchase.
    # + vendorPurchaseName - The name of the vendor purchase.
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The requested vendor purchase record was returned successfully. 
    remote isolated function getVendorPurchase(string companyId, string vendorPurchaseVendorId, string vendorPurchaseVendorNumber, string vendorPurchaseName, map<string|string[]> headers = {}, *GetVendorPurchaseQueries queries) returns VendorPurchase|error {
        string resourcePath = string `/companies(${getEncodedUri(companyId)})/vendorPurchases(${getEncodedUri(vendorPurchaseVendorId)},'${getEncodedUri(vendorPurchaseVendorNumber)}','${getEncodedUri(vendorPurchaseName)}')`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        return self.clientEp->get(resourcePath, headers);
    }
}
